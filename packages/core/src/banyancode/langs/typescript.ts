import type { ParseResult, ParsedNode, ParsedEdge } from "./types"

// stub - will be replaced
export function parseTypeScript(content: string, fileID: string, filePath: string, language: string): ParseResult {
  return { nodes: [], edges: [], imports: [] }
}
