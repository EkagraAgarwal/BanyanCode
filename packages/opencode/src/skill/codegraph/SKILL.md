---
name: codegraph
description: Polyglot code graph and GraphRAG retrieval for BanyanCode agents. Use codegraph_query, codegraph_callers, codegraph_dependents, codegraph_impact, and code_search to navigate the code graph. code_search uses FTS5 lexical + optional vector + graph expansion by default (hybrid mode). Use codegraph_status to check the build state. Use codegraph_build and code_embed_update to populate the graph.
---

